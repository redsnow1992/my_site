<div class="linkslist">
  <p class="asidetitle"><%= __('links') %></p>
    <ul>
      <% for (var i in theme.links){ %>
        <li>
          <%- link_to(theme.links[i].site, theme.links[i].title, {external: true}) %>
        </li>
      <% } %>
    </ul>
</div>
